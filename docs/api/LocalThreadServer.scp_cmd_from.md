### [LocalThreadServer](LocalThreadServer.md).scp_cmd_from (property)




scp (cp in this case) command to copy files from the server directory.

Returns
----------
* (string): command with "{}" placeholders for `server_path` and `local_path`.

