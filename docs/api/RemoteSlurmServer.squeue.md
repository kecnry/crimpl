### [RemoteSlurmServer](RemoteSlurmServer.md).squeue (property)




Run and return the output of `squeue` on the server (for all jobs).

To run for a single job, see [RemoteSlurmJob.squeue](RemoteSlurmJob.squeue.md).

Returns
-----------
* (string)

